<main style="text-align: center;">

	<h1>編集ページ</h1>

	<%= render 'form', artist: @artist %>

	<%= link_to '登録情報確認', [:admin, @artist],class: "btn btn-primary" %> |
	<%= link_to '戻る', admin_artists_path,class: "btn btn-primary" %>
</main>